Sharp Grotesk Book 20 — installed and live.

  SharpGrotesk-Book20.woff2   34 KB  ← what browsers actually load
  SharpGrotesk-Book20.woff    38 KB  ← fallback for older browsers
  SharpGrotesk-Book20.otf     52 KB  ← original source, kept for reference

Licensed from Sharp Type. Roman only — no italic in this licence, so heading
italic accents use Archivo Italic (set explicitly in css/site.css).

If you later license the italic:
  1. convert it to SharpGrotesk-Book20Italic.woff2
  2. add a matching @font-face with font-style:italic
  3. change the `em` rule in the TYPE SYSTEM block to var(--f-display)
